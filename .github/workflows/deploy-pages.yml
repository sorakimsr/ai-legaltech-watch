name: Deploy Pages (on push)

# 정적 파일(HTML/CSS/JS) commit 시 GitHub Pages 즉시 배포.
# 데이터 수집은 별도 daily-update.yml 에서 cron으로 처리.

on:
  push:
    branches: [main]
    paths:
      - "index.html"
      - "404.html"
      - "app.js"
      - "app.util.js"      # v6.15.32: 누락돼 있어 이 파일만 수정 시 자동 배포 안 되던 버그 수정
      - "styles.css"
      - "assets/**"        # v6.15.32: 로고·아이콘 등 정적 자산 변경도 배포 트리거
      - "data/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

# v6.18.0 — 트렌드 연속성 엔진 + ACTION 재설계 (2026-07-07)

## 계기

v6.17.0(구조 개선) 이후 남은 실질 퀄리티 병목 2가지에 대한 대응 (사용자 결정: A+B 동시):

- **A. daily 카드가 '트렌드'가 아니라 '당일 뉴스 해설'** — 매일 히스토리 없이 처음부터 생성돼 같은 거시 트렌드(예: AI 거버넌스 제도화)가 5/26·5/27·5/28 연달아 재도출. "이 흐름이 N일째 가속, 오늘 달라진 것은 X"라는 진짜 트렌드 분석 불가.
- **B. ACTION 템플릿화 + 페르소나 표류** — 거의 모든 action이 "분류/매핑 → 파일럿 → KPI 설정" 한 틀. "자사 AI 프로젝트"·"사내 계약서" 등 일반 기업 관점 표현이 대형로펌 경영전략팀 페르소나와 불일치.

## 변경

### A. 트렌드 연속성 엔진 (`strategy_quality.py` + `generate_strategy.py`)

- `load_recent_daily_context()` — strategy_history.json에서 최근 7일 daily 카드의 주제 목록(최대 40) + action 예시(최대 6) 추출. `_emergency` 제외, 옛 list 포맷 호환.
- Stage A(클러스터링)에 히스토리 블록 주입 → 클러스터별 `continuity` 판정:
  - `new` / `developing`(전개 — **오늘의 delta가 카드 가치의 전부**) / `reignited`(재점화)
  - developing/reignited는 `prior_ref`(이어받는 기존 카드 제목) 기입
  - **"기존 카드와 같은 주제 + 오늘 새 진전 없음 → 클러스터화 금지"** (동어반복 카드 방지)
- Stage B(카드 작성)에 연속성 블록 주입 — 전개 카드는 흐름의 연속임을 명시하고 delta 중심 서술 강제.
- `continuity`/`prior_ref`가 최종 카드 → history/news.json에 보존 (UI에서 "전개 N일째" 배지 등에 활용 가능 — 프런트 반영은 별도).

### B. ACTION 재설계

- CARD_PROMPT action 규칙 전면 교체 — **로펌 경영전략팀 4개 렌즈 중 하나 선택**:
  ① 자문 기회(클라이언트군·자문 상품·세미나·법리 메모) ② 프랙티스 전략(수임 구조·경쟁 로펌 포지셔닝) ③ 로펌 내부 도입 ④ 모니터링(움직일 트리거 조건 명시)
- 금지 템플릿 명시: "분류/매핑", "파일럿 진행", "KPI 설정", "체크리스트 개발" 류 + "자사"·"사내" 일반 기업 관점 표현.
- 최근 카드 action을 **negative example로 프롬프트에 주입** (같은 문형 반복 = 실패작).
- `flag_template_actions()` — deterministic 이중 방어: 최근 히스토리·같은 배치 action과 토큰 Jaccard ≥ 0.45면 `_action_template` 플래그 + 로그 (drop 아님 — 반복 비율이 프롬프트 재조정의 정량 신호).
- 레거시 PROMPT_TEMPLATE(weekly/monthly·폴백)에도 로펌 렌즈 규칙 반영.

### 테스트

`test_strategy_quality.py` 16 → 24 tests. 전체 40 passed (회귀 없음). 통합 mock 검증: 히스토리 주입 → 연속성 라벨 → 최종 카드 메타데이터 → 템플릿 감지 전 구간 확인.

## 운영 노트

- 첫 빌드는 기존 히스토리(v6.17 이전 카드)를 이어받으므로 developing 라벨이 과다/과소할 수 있음 — 2~3일 관찰 권장.
- 모니터링: 빌드 로그 `[연속성] 전개 N · 재점화 N · 신규 N`, `[action 템플릿] N/M건`.
- 다음 슬라이스 후보: C(사실 검증 게이트 — 카드 주장 ↔ 근거 기사 대조), D(사이트 👍/👎 피드백 루프 + LLM 가상 대표 평가), UI 연속성 배지.
